// ~/.config/zellij/layouts/default.kdl
// No tab-bar (top), no pane frames -- just the bottom key/status bar.

layout {
    default_tab_template {
        children
        pane size=2 borderless=true {
            plugin location="zellij:status-bar"
        }
    }
}
