# global_constants.nix
#
# Valores compartilhados por todos os hosts (NixOS e o home-manager
# standalone do macbook). Hoje só o username — é aqui que qualquer
# outra constante de verdade global (não específica de host) deveria
# entrar, no lugar de ficar espalhada.
{
  username = "borba";
}
