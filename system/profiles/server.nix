# system/profiles/server.nix
#
# Placeholder — nenhum host hoje é headless (os 4 hosts NixOS são todos
# desktops com sessão niri). Existe só pra bater com a forma da
# estrutura (system/profiles/{base,desktop,server}.nix); fica vazio até
# o dia em que houver um host servidor de verdade.
_: {}
