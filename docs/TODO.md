# TODO

- [ ] Wire `assets/zen.user.js` into Home Manager activation so `home-manager switch` installs it to active Zen Flatpak profile (`~/.var/app/app.zen_browser.zen/.zen/.../user.js`).
- [ ] Wire `assets/floorp.user.js` into Home Manager activation so `home-manager switch` installs it to active Floorp Flatpak profile (`~/.var/app/one.ablaze.floorp/.floorp/.../user.js`).
- [ ] Keep profile resolution dynamic via each browser `profiles.ini` / install mapping; avoid hardcoded hashed profile directories.
